title: The way of heaven
description: .....
template:
  repo: the-way-of-heaven-template
  name: the-way-of-heaven
before:
  - type: createIssue
    title: Welcome!
    body: new-issue.md
steps:
  - title: First step
    description: The first step in your course
    event: issues.assigned
    link: '{{ repoUrl }}'
    actions:
      - type: closeIssue
